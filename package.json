{
  "name": "stellar_card-repo-checks",
  "private": true,
  "description": "Repo-wide configuration checks (dependabot, CI workflows, docs, docker, husky) — not published, not part of any sub-package.",
  "scripts": {
    "test": "vitest run test/"
  },
  "devDependencies": {
    "vitest": "^3.2.7",
    "yaml": "^2.8.1"
  }
}
